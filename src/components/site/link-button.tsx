/* LinkButton lives in the design system now (src/components/ds/actions.tsx).
   This shim keeps the old import path working; import from "@/components/ds". */
export { LinkButton } from "@/components/ds";
